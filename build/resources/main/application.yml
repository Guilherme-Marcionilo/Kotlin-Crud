micronaut:
  server:
    cors:
      enabled: true
  application:
    name: toyProject

endpoints:
  health:
    enabled: true

cassandra:
  default:
    basic:
      contact-points:
        - "172.30.224.1:9042"
      session-keyspace: toy
      load-balancing-policy:
        local-datacenter: datacenter1